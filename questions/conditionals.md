## Level : Beginner

1. Write a program that checks if a number is positive.
2. Ask the user for their age and print whether they are an adult (18+) or a minor.
3. Check if a given string is equal to `"python"`.
4. Given two numbers, print the larger one.
5. Check if a number is even or odd.
6. Check if the entered character is a vowel or consonant.
7. Given a password string, check if its length is at least 8.
8. Check if a number is divisible by 3.
9. Given three numbers, print the largest.
10. Given a list of numbers, check if the list is empty or not.
11. Make a list of numbers and check if the first element is greater than the last element.
12. Given a string, check if it contains the substring `"py"`.
13. Create a program that checks if the length of a given list is even or odd.
14. Ask for age:

    * If age < 18 → print "Minor"
    * If 18–59 → print "Adult"
    * If 60+ → print "Senior"
15. Take input for a username:

    * if length < 3 → reject
    * if contains spaces → reject
    * else accept

## Level : Moderate 

1. Given a year, determine if it is a leap year.
   Rules For leap year :
   * It is divisible by 400
   * It is divisible by 4 and not divisible by 100

2. 
