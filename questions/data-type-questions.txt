

1. Create a string "python programming". Convert it into uppercase, titlecase, and replace "python" with "java".


2. Given s = "  hello world  ", remove extra spaces on both sides and check its length.


3. Let text = "banana". Count how many times "a" appears and find the index of first "n".


4. From msg = "WelcomeToPython", extract the substring "To".


5. Join the list ['apple', 'banana', 'kiwi'] into a single string separated by commas.


6. Using [3, 1, 4, 1, 5], append 9, insert 6 at index 2, and remove the first 1.


7. Sort [50, 10, 40, 20] in ascending order and in descending order.


8. In colors = ['red', 'blue', 'green'], replace "blue" with "yellow".


9. Combine a = [1,2,3] and b = [4,5] into one list.


10. Using fruits = ["apple", "orange", "banana"], find the length of the list and the index of "banana".


11. Convert (4, 5, 6, 7) into a list and add number 8 at the end.


12. For items = (10, 20, 10, 30, 10), count how many times 10 appears.


13. Find the index of value 30 in (40, 30, 20, 30).


14. Let person = {"name": "Ali", "age": 25}. Add "city": "Delhi" and update "age" to 26.


15. From student = {"math": 90, "english": 85, "science": 92}, get only the keys and only the values.


16. Remove the key "english" from the above dictionary and return its value.


17. Merge:

a = {"x": 1, "y": 2}

b = {"y": 3, "z": 4}
Use the method that overrides duplicate keys.



18. Using s1 = {1, 2, 3} and s2 = {3, 4, 5}, find the union, intersection, and difference s1 - s2.


19. From numbers = {1, 2, 3, 4, 5}, remove 3 using a method that does not raise an error if the element is missing.


20. Add multiple elements [30, 40, 50] at once into setA = {10, 20}.


21. Convert "123.45" into a float and then into an integer.


22. Round 7.8973 to 2 decimal places and to 0 decimal places.


23. Find the absolute value of -76 and compute pow(3, 4) without using **.




---

24. Extract the 4th character from "technology" and convert it to uppercase.


25. Reverse the string "Python" using slicing.


26. Split "a,b,c,d,e" into a list and then join it back without commas.


27. Replace "@" with "" (empty string) in "he@ll@o@@".


28. Access the last element of the list [10, 20, 30, 40] without using loops.


29. Remove all occurrences of "red" from the list ["red", "blue", "red", "green"] using a method.


30. Copy the list [1,2,3] into a new list and append 4 to the new list without affecting the original.


31. Count how many times "python" appears in the list ["python", "java", "python", "c"].


32. Convert the list [45, 55, 65] into a tuple.


33. Check whether 50 exists in (10, 20, 30, 40) using a method.


34. Concatenate two tuples (1,2) and (3,4,5) into a single tuple.


35. Convert the tuple (100, 200, 300) into a single string separated by hyphens.


36. Create a dictionary using keys: "name", "course", "year" and assign any values.


37. Print only the keys of {"a":1, "b":2, "c":3} as a list.


38. Using info = {"brand":"Nike","price":2000}, update the price to 1800 using the update method.


39. Get the value of "math" from {"math":95,"science":90} without using square brackets.


40. Duplicate the dictionary {"x":9, "y":8} into a new one and change "y" to 5 without affecting the original.


41. Check if 4 exists in the set {1,2,3,4} using a method.


42. Find the symmetric difference between {1,2,3} and {3,4,5}.


43. Convert list [1,1,2,2,3,3] into a set.


44. From {10,20,30,40}, remove an element randomly using a method.


45. Add the element 100 to {50,60,70}.


46. Convert "456" into an integer and calculate its square.


47. Convert integer 55 into a string and concatenate with "kg".


48. Find the minimum and maximum values in the list [9,4,7,2,8] using functions.


49. Compute square root of 64 using the correct function from the math module.


50. Using price = 199.98765, format and print it to 2 decimal places as a string.


