

1. **Input:** `word = "Python"`
   **Task:** Print the first and last characters.
   **Expected Output:**

   ```
   P
   n
   ```

2. **Input:** `name = "Computer"`
   **Task:** Print the 2nd and 4th characters.
   **Expected Output:**

   ```
   o
   p
   ```

3. **Input:** `text = "Programming"`
   **Task:** Print the character at index 3.
   **Expected Output:**

   ```
   g
   ```

4. **Input:** `city = "Mumbai"`
   **Task:** Print each character on a new line.
   **Expected Output:**

   ```
   M
   u
   m
   b
   a
   i
   ```

5. **Input:** `string = "Developer"`
   **Task:** Print the length of the string without using `len()`.
   **Expected Output:**

   ```
   9
   ```

6. **Input:** `word = "Laptop"`
   **Task:** Swap the first and last characters and print the result.
   **Expected Output:**

   ```
   taptoL
   ```

7. **Input:** `word = "Python"`
   **Task:** Print the first three characters.
   **Expected Output:**

   ```
   Pyt
   ```

8. **Input:** `text = "Programming"`
   **Task:** Print everything from index 2 to the end.
   **Expected Output:**

   ```
   ogramming
   ```

9. **Input:** `msg = "HelloWorld"`
   **Task:** Print every second character.
   **Expected Output:**

   ```
   Hlool
   ```

10. **Input:** `word = "Python"`
    **Task:** Print the string in reverse using slicing.
    **Expected Output:**

    ```
    nohtyP
    ```

11. **Input:** `string = "Elephant"`
    **Task:** Print the middle three characters.
    **Expected Output:**

    ```
    eph
    ```

12. **Input:** `sentence = "python is fun"`
    **Task:** Print it in both uppercase and lowercase.
    **Expected Output:**

    ```
    PYTHON IS FUN  
    python is fun
    ```

13. **Input:** `fruit = "banana"`
    **Task:** Count how many times `'a'` appears.
    **Expected Output:**

    ```
    3
    ```

14. **Input:** `sentence = "I love programming"`
    **Task:** Replace all spaces with underscores (`_`).
    **Expected Output:**

    ```
    I_love_programming
    ```

15. **Input:** `name = "apple"`
    **Task:** Check if it starts with `'A'` (case-insensitive).
    **Expected Output:**

    ```
    True
    ```

16. **Input:** `data = "   Hello World   "`
    **Task:** Remove spaces from both ends.
    **Expected Output:**

    ```
    Hello World
    ```

17. **Input:** `sentence = "Python is powerful"`
    **Task:** Check if it contains the word `"python"` (case-insensitive).
    **Expected Output:**

    ```
    True
    ```

18. **Input:** `word = "Education"`
    **Task:** Count how many vowels it has.
    **Expected Output:**

    ```
    5
    ```

19. **Input:** `sentence = "Python makes coding easy"`
    **Task:** Print each word on a new line.
    **Expected Output:**

    ```
    Python
    makes
    coding
    easy
    ```

20. **Input:** `text = "madam"`
    **Task:** Check if it’s a palindrome.
    **Expected Output:**

    ```
    True
    ```

21. **Input:** `sentence = "Python is easy to learn"`
    **Task:** Count how many words it contains using `.split()`.
    **Expected Output:**

    ```
    5
    ```

22. **Input:** `text = "Education"`
    **Task:** Replace all vowels with `"*"`.
    **Expected Output:**

    ```
    *d*c*t**n
    ```

23. **Input:** `sentence = "python is fun"`
    **Task:** Capitalize the first letter of each word.
    **Expected Output:**

    ```
    Python Is Fun
    ```

24. **Input:** `text = "python is fun"`
    **Task:** Convert it to `"PYTHON-IS-FUN"` using string methods.
    **Expected Output:**

    ```
    PYTHON-IS-FUN
    ```

25. **Input:** `word = "HELLO"`
    **Task:** Check if it’s all uppercase.
    **Expected Output:**

    ```
    True
    ```

26. **Input:** `string = "abc123xyz"`
    **Task:** Count how many digits it contains.
    **Expected Output:**

    ```
    3
    ```

27. **Input:** `sentence = "learn python step by step"`
    **Task:** Print it in title case, then replace spaces with `-`.
    **Expected Output:**

    ```
    Learn-Python-Step-By-Step
    ```

28. **Input:** `text = "Hello, World! Python is cool."`
    **Task:** Remove all punctuation marks (`.,!?`).
    **Expected Output:**

    ```
    Hello World Python is cool
    ```

29. **Input:** `text = "Python 3 is great  "`
    **Task:** Count alphabets, digits, and spaces.
    **Expected Output:**

    ```
    Alphabets: 10  
    Digits: 1  
    Spaces: 4
    ```

30. **Input:** `text = "Never Odd Or Even"`
    **Task:** Convert to lowercase, remove spaces, and check if it’s a palindrome.
    **Expected Output:**

    ```
    True
    ```

---
